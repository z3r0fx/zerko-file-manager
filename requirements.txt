fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.6
jinja2==3.1.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
sqlalchemy==2.0.50
aiosqlite==0.19.0
python-magic==0.4.27
Pillow==12.2.0
numpy==2.4.6
opencv-python-headless==4.13.0.92
psutil==5.9.8
# main.py imports this for the upload lock. It was missing here, so a fresh
# install crashed on startup with ModuleNotFoundError while existing installs
# carried on working - the worst kind of gap to leave in a dependency list.
filelock==3.16.1
# openai-whisper requires torch which may not have Python 3.14 wheels yet.
# Install separately if needed: pip install openai-whisper
# openai-whisper==20231117
